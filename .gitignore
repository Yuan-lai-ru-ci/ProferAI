# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
*.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# ignore craft-agents-oss folder
craft-agents-oss

# ignore what_are_humans_thinking folder
what_are_humans_thinking

# vendor binaries
vendor

# git worktrees (managed by proma-worktree skill)
.worktrees

# 本地工作区辅助目录（含嵌套 worktree，不入库）
.claude/

# 打包内置二进制(体积大，构建产物，不入库)
apps/electron/resources/bin/

# SQLite 数据库文件（不入库）
*.db
*.db-wal
*.db-shm
*.sqlite

# Profer 开发日志与临时文件
.context/*.log
.context/*.err.log
.context/*.out.log
.context/*.pid
.context/*.ps1
.context/*.diff
# 任意层级 .context 目录下都忽略调试残留（图片/日志）
.context/**/*.png
.context/**/*.log
.context/**/*.err.log
.context/**/*.out.log
**/.context/**/*.log
**/.context/**/*.err.log
**/.context/**/*.out.log
**/.context/**/*.png
**/.context/**/*.log.err
**/.context/**/*.out.err
**/.context/**/*.log.out
**/.context/**/*.err
nul
scripts/_fix_define2.py

# Pi upgrade / package 中途备份与运行日志（不纳入版本控制）
package.json.bak-*
patches/*.patch.bak-*
dev-run.log
dev-start*.log
*.dev-start*.log
*.upgrade-backup

# Tablet remote 开发模式目录与本地产物（不入库/不打包）
apps/electron/.profer-dev/
apps/electron/remote-test-*.log
apps/electron/dist/renderer/tablet/
*.remote-token.json

# Tablet App（Capacitor 壳）：原生平台工程、同步产物与本地 APK 不入库
tablet-app/android/
tablet-app/web/
tablet-app/node_modules/
tablet-app/dist/

# local Profer Team Server deployment (keep separate repo)
server/
