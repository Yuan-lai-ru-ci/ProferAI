diff --git a/dist/utils/oauth/openai-codex.d.ts b/dist/utils/oauth/openai-codex.d.ts
index 2edcbef..433cf42 100644
--- a/dist/utils/oauth/openai-codex.d.ts
+++ b/dist/utils/oauth/openai-codex.d.ts
@@ -14,6 +14,8 @@ export declare const OPENAI_CODEX_DEVICE_CODE_LOGIN_METHOD = "device_code";
 export declare function loginOpenAICodexDeviceCode(options: {
     onDeviceCode: (info: OAuthDeviceCodeInfo) => void;
     signal?: AbortSignal;
+    /** Optional fetch implementation for OAuth HTTP requests. */
+    fetch?: typeof globalThis.fetch;
 }): Promise<OAuthCredentials>;
 /**
  * Login with OpenAI Codex OAuth
@@ -35,11 +37,16 @@ export declare function loginOpenAICodex(options: {
     onProgress?: (message: string) => void;
     onManualCodeInput?: () => Promise<string>;
     originator?: string;
+    /** Optional fetch implementation for OAuth token exchange. */
+    fetch?: typeof globalThis.fetch;
 }): Promise<OAuthCredentials>;
 /**
  * Refresh OpenAI Codex OAuth token
  */
-export declare function refreshOpenAICodexToken(refreshToken: string): Promise<OAuthCredentials>;
+export declare function refreshOpenAICodexToken(refreshToken: string, options?: {
+    /** Optional fetch implementation for OAuth token refresh. */
+    fetch?: typeof globalThis.fetch;
+}): Promise<OAuthCredentials>;
 export declare const openaiCodexOAuth: OAuthAuth;
 export declare const openaiCodexOAuthProvider: OAuthProviderInterface;
 //# sourceMappingURL=openai-codex.d.ts.map
\ No newline at end of file
diff --git a/dist/utils/oauth/openai-codex.js b/dist/utils/oauth/openai-codex.js
index 3b59251..bbce2d8 100644
--- a/dist/utils/oauth/openai-codex.js
+++ b/dist/utils/oauth/openai-codex.js
@@ -82,9 +82,9 @@ function decodeJwt(token) {
         return null;
     }
 }
-async function fetchWithLoginCancellation(input, init) {
+async function fetchWithLoginCancellation(fetchFn, input, init) {
     try {
-        return await fetch(input, init);
+        return await fetchFn(input, init);
     }
     catch (error) {
         if (init.signal?.aborted) {
@@ -109,8 +109,8 @@ async function readTokenResponse(response, operation) {
         expires: Date.now() + json.expires_in * 1000,
     };
 }
-async function exchangeAuthorizationCode(code, verifier, redirectUri = REDIRECT_URI, signal) {
-    const response = await fetchWithLoginCancellation(TOKEN_URL, {
+async function exchangeAuthorizationCode(code, verifier, redirectUri = REDIRECT_URI, signal, fetchFn = fetch) {
+    const response = await fetchWithLoginCancellation(fetchFn, TOKEN_URL, {
         method: "POST",
         headers: { "Content-Type": "application/x-www-form-urlencoded" },
         body: new URLSearchParams({
@@ -124,10 +124,10 @@ async function exchangeAuthorizationCode(code, verifier, redirectUri = REDIRECT_
     });
     return readTokenResponse(response, "exchange");
 }
-async function refreshAccessToken(refreshToken) {
+async function refreshAccessToken(refreshToken, fetchFn = fetch) {
     let response;
     try {
-        response = await fetch(TOKEN_URL, {
+        response = await fetchFn(TOKEN_URL, {
             method: "POST",
             headers: { "Content-Type": "application/x-www-form-urlencoded" },
             body: new URLSearchParams({
@@ -142,8 +142,8 @@ async function refreshAccessToken(refreshToken) {
     }
     return readTokenResponse(response, "refresh");
 }
-async function startOpenAICodexDeviceAuth(signal) {
-    const response = await fetchWithLoginCancellation(DEVICE_USER_CODE_URL, {
+async function startOpenAICodexDeviceAuth(signal, fetchFn = fetch) {
+    const response = await fetchWithLoginCancellation(fetchFn, DEVICE_USER_CODE_URL, {
         method: "POST",
         headers: { "Content-Type": "application/json" },
         body: JSON.stringify({ client_id: CLIENT_ID }),
@@ -172,13 +172,13 @@ async function startOpenAICodexDeviceAuth(signal) {
         intervalSeconds,
     };
 }
-async function pollOpenAICodexDeviceAuth(device, signal) {
+async function pollOpenAICodexDeviceAuth(device, signal, fetchFn = fetch) {
     return pollOAuthDeviceCodeFlow({
         intervalSeconds: device.intervalSeconds,
         expiresInSeconds: DEVICE_CODE_TIMEOUT_SECONDS,
         signal,
         poll: async () => {
-            const response = await fetchWithLoginCancellation(DEVICE_TOKEN_URL, {
+            const response = await fetchWithLoginCancellation(fetchFn, DEVICE_TOKEN_URL, {
                 method: "POST",
                 headers: { "Content-Type": "application/json" },
                 body: JSON.stringify({
@@ -334,22 +334,22 @@ function credentialsFromToken(token) {
         accountId,
     };
 }
-async function exchangeAuthorizationCodeForCredentials(code, verifier, redirectUri, signal) {
-    return credentialsFromToken(await exchangeAuthorizationCode(code, verifier, redirectUri, signal));
+async function exchangeAuthorizationCodeForCredentials(code, verifier, redirectUri, signal, fetchFn = fetch) {
+    return credentialsFromToken(await exchangeAuthorizationCode(code, verifier, redirectUri, signal, fetchFn));
 }
 /**
  * Login with OpenAI Codex OAuth using the Codex device-code flow.
  */
 export async function loginOpenAICodexDeviceCode(options) {
-    const device = await startOpenAICodexDeviceAuth(options.signal);
+    const device = await startOpenAICodexDeviceAuth(options.signal, options.fetch);
     options.onDeviceCode({
         userCode: device.userCode,
         verificationUri: DEVICE_VERIFICATION_URI,
         intervalSeconds: device.intervalSeconds,
         expiresInSeconds: DEVICE_CODE_TIMEOUT_SECONDS,
     });
-    const code = await pollOpenAICodexDeviceAuth(device, options.signal);
-    return exchangeAuthorizationCodeForCredentials(code.authorizationCode, code.codeVerifier, DEVICE_REDIRECT_URI, options.signal);
+    const code = await pollOpenAICodexDeviceAuth(device, options.signal, options.fetch);
+    return exchangeAuthorizationCodeForCredentials(code.authorizationCode, code.codeVerifier, DEVICE_REDIRECT_URI, options.signal, options.fetch);
 }
 /**
  * Login with OpenAI Codex OAuth
@@ -435,7 +435,7 @@ export async function loginOpenAICodex(options) {
         if (!code) {
             throw new Error("Missing authorization code");
         }
-        return exchangeAuthorizationCodeForCredentials(code, verifier, REDIRECT_URI);
+        return exchangeAuthorizationCodeForCredentials(code, verifier, REDIRECT_URI, undefined, options.fetch);
     }
     finally {
         server.close();
@@ -444,8 +444,8 @@ export async function loginOpenAICodex(options) {
 /**
  * Refresh OpenAI Codex OAuth token
  */
-export async function refreshOpenAICodexToken(refreshToken) {
-    return credentialsFromToken(await refreshAccessToken(refreshToken));
+export async function refreshOpenAICodexToken(refreshToken, options = {}) {
+    return credentialsFromToken(await refreshAccessToken(refreshToken, options.fetch));
 }
 export const openaiCodexOAuth = {
     name: "OpenAI (ChatGPT Plus/Pro)",
diff --git a/dist/utils/retry.js b/dist/utils/retry.js
index fd52e8b..0236d4a 100644
--- a/dist/utils/retry.js
+++ b/dist/utils/retry.js
@@ -55,6 +55,7 @@ const RETRYABLE_PROVIDER_ERROR_PATTERN = buildProviderErrorPattern([
     // "stream ended without ..." and "Anthropic stream ended before message_stop"
     // (#4433); Bedrock/Smithy can throw an HTTP/2 no-response error (#3594).
     "ended without",
+    "upstream request failed",
     "stream ended before message_stop",
     "http2 request did not get a response",
     // Provider-requested retry delay cap failures should flow through the outer
