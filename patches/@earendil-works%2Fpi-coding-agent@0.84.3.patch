diff --git a/dist/core/agent-session.d.ts b/dist/core/agent-session.d.ts
index 528a246..36666aa 100644
--- a/dist/core/agent-session.d.ts
+++ b/dist/core/agent-session.d.ts
@@ -69,15 +69,38 @@ export type AgentSessionEvent = Exclude<AgentEvent, {
     willRetry: boolean;
     errorMessage?: string;
 } | {
+    /** Retry has been scheduled and is currently backing off. */
     type: "auto_retry_start";
     attempt: number;
     maxAttempts: number;
+    totalAttempt: number;
+    maxTotalAttempts: number;
     delayMs: number;
+    totalDelayMs: number;
+    maxTotalDelayMs: number;
+    errorMessage: string;
+} | {
+    /** Backoff completed; a retry request will start immediately. */
+    type: "auto_retry_attempt_start";
+    attempt: number;
+    maxAttempts: number;
+    totalAttempt: number;
+    maxTotalAttempts: number;
+    delayMs: number;
+    totalDelayMs: number;
+    maxTotalDelayMs: number;
     errorMessage: string;
 } | {
     type: "auto_retry_end";
     success: boolean;
+    outcome: "succeeded" | "exhausted" | "cancelled";
     attempt: number;
+    maxAttempts: number;
+    totalAttempt: number;
+    maxTotalAttempts: number;
+    delayMs: number;
+    totalDelayMs: number;
+    maxTotalDelayMs: number;
     finalError?: string;
 } | {
     type: "summarization_retry_scheduled";
@@ -94,11 +117,6 @@ export type AgentSessionEvent = Exclude<AgentEvent, {
     reason: "manual" | "threshold" | "overflow";
 } | {
     type: "summarization_retry_finished";
-} | {
-    type: "auto_retry_end";
-    success: boolean;
-    attempt: number;
-    finalError?: string;
 } | {
     type: "bash_execution_update";
     id?: string;
@@ -216,6 +234,9 @@ export declare class AgentSession {
     private _branchSummaryAbortController;
     private _retryAbortController;
     private _retryAttempt;
+    private _retryRunAttempts;
+    private _retryRunDelayMs;
+    private _pendingRetryAttemptStart;
     private readonly _bashAbortControllers;
     private _pendingBashMessages;
     private _extensionRunner;
@@ -269,6 +290,8 @@ export declare class AgentSession {
     private _lastAssistantMessage;
     /** Internal handler for agent events - shared by subscribe and reconnect */
     private _handleAgentEvent;
+    private _canScheduleRetry;
+    private _getRetryEventDetails;
     private _willRetryAfterAgentEnd;
     /** Find the last assistant message in agent state (including aborted ones) */
     private _findLastAssistantMessage;
diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index f4fd9f3..9813d8b 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -99,7 +99,14 @@ export class AgentSession {
     _branchSummaryAbortController = undefined;
     // Retry state
     _retryAbortController = undefined;
+    /** Retries scheduled in the current consecutive-failure segment. */
     _retryAttempt = 0;
+    /** Retries scheduled across all model calls of one top-level prompt. */
+    _retryRunAttempts = 0;
+    /** Total backoff delay consumed by one top-level prompt. */
+    _retryRunDelayMs = 0;
+    /** Retry details emitted immediately before agent.continue(). */
+    _pendingRetryAttemptStart = undefined;
     // Bash execution state
     _bashAbortControllers = new Set();
     _pendingBashMessages = [];
@@ -388,22 +395,43 @@ export class AgentSession {
                 if (assistantMsg.stopReason !== "error" && assistantMsg.stopReason !== "length") {
                     this._overflowRecoveryAttempted = false;
                 }
-                // Reset retry counter immediately on successful assistant response
-                // This prevents accumulation across multiple LLM calls within a turn
+                // Reset only the consecutive-failure counter. The total prompt budget
+                // remains in force, preventing tool loops from receiving fresh retries.
                 if (assistantMsg.stopReason !== "error" && this._retryAttempt > 0) {
+                    const settings = this.settingsManager.getRetrySettings();
+                    const retryCancelled = assistantMsg.stopReason === "aborted";
                     this._emit({
                         type: "auto_retry_end",
-                        success: true,
-                        attempt: this._retryAttempt,
+                        success: !retryCancelled,
+                        outcome: retryCancelled ? "cancelled" : "succeeded",
+                        ...this._getRetryEventDetails(this._retryAttempt, settings),
                     });
                     this._retryAttempt = 0;
                 }
             }
         }
     };
+    _canScheduleRetry(settings = this.settingsManager.getRetrySettings()) {
+        return settings.enabled &&
+            this._retryAttempt < settings.maxRetries &&
+            this._retryRunAttempts < settings.maxTotalRetries &&
+            this._retryRunDelayMs < settings.maxTotalDelayMs;
+    }
+    _getRetryEventDetails(attempt, settings, delayMs = 0, errorMessage) {
+        return {
+            attempt,
+            maxAttempts: settings.maxRetries,
+            totalAttempt: this._retryRunAttempts,
+            maxTotalAttempts: settings.maxTotalRetries,
+            delayMs,
+            totalDelayMs: this._retryRunDelayMs,
+            maxTotalDelayMs: settings.maxTotalDelayMs,
+            ...(errorMessage ? { errorMessage } : {}),
+        };
+    }
     _willRetryAfterAgentEnd(event) {
         const settings = this.settingsManager.getRetrySettings();
-        if (!settings.enabled || this._retryAttempt >= settings.maxRetries) {
+        if (!this._canScheduleRetry(settings)) {
             return false;
         }
         for (let i = event.messages.length - 1; i >= 0; i--) {
@@ -746,13 +774,24 @@ export class AgentSession {
     // =========================================================================
     async _runAgentPrompt(messages) {
         this._isAgentRunActive = true;
+        // Reset total retry budget for every top-level prompt.
+        this._retryAttempt = 0;
+        this._retryRunAttempts = 0;
+        this._retryRunDelayMs = 0;
+        this._pendingRetryAttemptStart = undefined;
         try {
             await this.agent.prompt(messages);
             while (await this._handlePostAgentRun()) {
+                const retryAttemptStart = this._pendingRetryAttemptStart;
+                this._pendingRetryAttemptStart = undefined;
+                if (retryAttemptStart) {
+                    this._emit({ type: "auto_retry_attempt_start", ...retryAttemptStart });
+                }
                 await this.agent.continue();
             }
         }
         finally {
+            this._pendingRetryAttemptStart = undefined;
             this._systemPromptOverride = undefined;
             this._flushPendingBashMessages();
             await this._emitAgentSettled();
@@ -768,10 +807,12 @@ export class AgentSession {
             return true;
         }
         if (msg.stopReason === "error" && this._retryAttempt > 0) {
+            const settings = this.settingsManager.getRetrySettings();
             this._emit({
                 type: "auto_retry_end",
                 success: false,
-                attempt: this._retryAttempt,
+                outcome: "exhausted",
+                ...this._getRetryEventDetails(this._retryAttempt, settings),
                 finalError: msg.errorMessage,
             });
             this._retryAttempt = 0;
@@ -2210,23 +2251,21 @@ export class AgentSession {
      */
     async _prepareRetry(message) {
         const settings = this.settingsManager.getRetrySettings();
-        if (!settings.enabled) {
-            return false;
-        }
-        this._retryAttempt++;
-        if (this._retryAttempt > settings.maxRetries) {
-            // Preserve the completed attempt count so post-run handling can emit the final failure.
-            this._retryAttempt--;
+        if (!this._canScheduleRetry(settings)) {
             return false;
         }
-        const delayMs = settings.baseDelayMs * 2 ** (this._retryAttempt - 1);
-        this._emit({
-            type: "auto_retry_start",
-            attempt: this._retryAttempt,
-            maxAttempts: settings.maxRetries,
-            delayMs,
-            errorMessage: message.errorMessage || "Unknown error",
-        });
+        const attempt = this._retryAttempt + 1;
+        const totalAttempt = this._retryRunAttempts + 1;
+        const nominalDelayMs = settings.baseDelayMs * 2 ** (attempt - 1);
+        const jitterMultiplier = 1 + (Math.random() * 2 - 1) * settings.jitterRatio;
+        const jitteredDelayMs = Math.max(0, Math.round(nominalDelayMs * jitterMultiplier));
+        const remainingDelayMs = Math.max(0, settings.maxTotalDelayMs - this._retryRunDelayMs);
+        const delayMs = Math.min(jitteredDelayMs, remainingDelayMs);
+        this._retryAttempt = attempt;
+        this._retryRunAttempts = totalAttempt;
+        this._retryRunDelayMs += delayMs;
+        const retryDetails = this._getRetryEventDetails(attempt, settings, delayMs, message.errorMessage || "Unknown error");
+        this._emit({ type: "auto_retry_start", ...retryDetails });
         // Remove error message from agent state (keep in session for history)
         const messages = this.agent.state.messages;
         if (messages.length > 0 && messages[messages.length - 1].role === "assistant") {
@@ -2238,20 +2277,15 @@ export class AgentSession {
             await sleep(delayMs, this._retryAbortController.signal);
         }
         catch {
-            // Aborted during sleep - emit end event so UI can clean up
-            const attempt = this._retryAttempt;
+            // Aborted during sleep - emit a terminal cancellation event for the UI.
             this._retryAttempt = 0;
-            this._emit({
-                type: "auto_retry_end",
-                success: false,
-                attempt,
-                finalError: "Retry cancelled",
-            });
+            this._emit({ type: "auto_retry_end", success: false, outcome: "cancelled", ...retryDetails, finalError: "Retry cancelled" });
             return false;
         }
         finally {
             this._retryAbortController = undefined;
         }
+        this._pendingRetryAttemptStart = retryDetails;
         return true;
     }
     /**
diff --git a/dist/core/settings-manager.d.ts b/dist/core/settings-manager.d.ts
index ea655c8..21b6f90 100644
--- a/dist/core/settings-manager.d.ts
+++ b/dist/core/settings-manager.d.ts
@@ -17,8 +17,16 @@ export interface ProviderRetrySettings {
 }
 export interface RetrySettings {
     enabled?: boolean;
+    /** Maximum retries within one consecutive failure segment. */
     maxRetries?: number;
+    /** Base exponential-backoff delay. */
     baseDelayMs?: number;
+    /** Symmetric jitter ratio between 0 and 1; defaults to 0. */
+    jitterRatio?: number;
+    /** Total retry budget across every model call within one top-level prompt. */
+    maxTotalRetries?: number;
+    /** Cumulative backoff-delay budget in one top-level prompt. */
+    maxTotalDelayMs?: number;
     provider?: ProviderRetrySettings;
 }
 export type TuiMode = RendererTuiMode;
@@ -232,6 +240,9 @@ export declare class SettingsManager {
         enabled: boolean;
         maxRetries: number;
         baseDelayMs: number;
+        jitterRatio: number;
+        maxTotalRetries: number;
+        maxTotalDelayMs: number;
     };
     getHttpIdleTimeoutMs(): number;
     setHttpIdleTimeoutMs(timeoutMs: number): void;
diff --git a/dist/core/settings-manager.js b/dist/core/settings-manager.js
index a098f75..4220028 100644
--- a/dist/core/settings-manager.js
+++ b/dist/core/settings-manager.js
@@ -590,10 +590,16 @@ export class SettingsManager {
         this.save();
     }
     getRetrySettings() {
+        const jitterRatio = this.settings.retry?.jitterRatio ?? 0;
+        const maxTotalRetries = this.settings.retry?.maxTotalRetries;
+        const maxTotalDelayMs = this.settings.retry?.maxTotalDelayMs;
         return {
             enabled: this.getRetryEnabled(),
             maxRetries: this.settings.retry?.maxRetries ?? 3,
             baseDelayMs: this.settings.retry?.baseDelayMs ?? 2000,
+            jitterRatio: Number.isFinite(jitterRatio) ? Math.min(1, Math.max(0, jitterRatio)) : 0,
+            maxTotalRetries: Number.isFinite(maxTotalRetries) ? Math.max(0, Math.floor(maxTotalRetries)) : Number.MAX_SAFE_INTEGER,
+            maxTotalDelayMs: Number.isFinite(maxTotalDelayMs) ? Math.max(0, Math.floor(maxTotalDelayMs)) : Number.MAX_SAFE_INTEGER,
         };
     }
     getHttpIdleTimeoutMs() {
