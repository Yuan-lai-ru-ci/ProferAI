{
  "name": "profer-tablet",
  "version": "0.1.0",
  "description": "Profer 移动版客户端（Capacitor 壳，装载 Profer 桌面端 remote-service 的移动端 Web UI）",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:web": "node scripts/sync-web.mjs",
    "build": "node scripts/sync-web.mjs && npx cap sync android",
    "apk": "node scripts/sync-web.mjs && npx cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/preferences": "^7.0.0"
  }
}
